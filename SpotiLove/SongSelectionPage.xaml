<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:SpotiLove"
             x:Class="SpotiLove.SongSelectionPage"
             BackgroundColor="#121212">

    <Grid RowDefinitions="Auto,Auto,Auto,*,Auto,Auto" Padding="20">

        <!-- Hidden Spotify Web Player (1x1 pixel, audio only) -->
        <local:SpotifyWebPlayer
            x:Name="SpotifyPlayer"
            Grid.Row="5"
            HeightRequest="1"
            WidthRequest="1"
            HorizontalOptions="Start"
            VerticalOptions="Start"
            IsVisible="False"/>

        <!-- Header -->
        <VerticalStackLayout Grid.Row="0" Spacing="10" Margin="0,20,0,15">
            <Label
                Text="Choose Your Favorite Songs"
                FontSize="28"
                FontAttributes="Bold"
                TextColor="White"
                HorizontalOptions="Center"/>
            <Label
                Text="Select at least 5 songs from your favorite artists"
                FontSize="16"
                TextColor="#b3b3b3"
                HorizontalOptions="Center"/>
            <Label
                x:Name="SelectedCountLabel"
                Text="0 selected"
                FontSize="14"
                TextColor="#1db954"
                HorizontalOptions="Center"
                Margin="0,5,0,0"/>
        </VerticalStackLayout>

        <!-- Progress Indicator -->
        <Frame
            Grid.Row="1"
            BackgroundColor="#212121"
            CornerRadius="10"
            Padding="15"
            Margin="0,0,0,15"
            HasShadow="False">
            <FlexLayout Direction="Row" JustifyContent="SpaceBetween" AlignItems="Center">
                <Label Text="🎤 Artists ✓" FontSize="14" TextColor="#1db954"/>
                <Label Text="🎵 Songs" FontSize="14" TextColor="White" FontAttributes="Bold"/>
            </FlexLayout>
        </Frame>

        <!-- Search Bar -->
        <Border
            Grid.Row="2"
            BackgroundColor="#212121"
            Stroke="#535353"
            StrokeThickness="1"
            StrokeShape="RoundRectangle 25"
            Margin="0,0,0,15">
            <SearchBar
                x:Name="SongSearchBar"
                Placeholder="Search songs..."
                PlaceholderColor="#b3b3b3"
                TextColor="White"
                BackgroundColor="Transparent"
                TextChanged="OnSearchTextChanged"/>
        </Border>

        <!-- Songs List -->
        <ScrollView Grid.Row="3">
            <VerticalStackLayout Spacing="15">
                <CollectionView
                    x:Name="SongsCollection"
                    SelectionMode="None">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Border
                                Stroke="{Binding BorderColor}"
                                StrokeThickness="2"
                                StrokeShape="RoundRectangle 12"
                                Background="#212121"
                                Margin="0,5"
                                Padding="15">

                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer Tapped="OnSongTapped" CommandParameter="{Binding .}"/>
                                </Border.GestureRecognizers>

                                <Grid ColumnDefinitions="Auto,*,Auto">

                                    <!-- Play / Pause Button -->
                                    <Border
                                        Grid.Column="0"
                                        StrokeThickness="0"
                                        Padding="0"
                                        HeightRequest="44"
                                        WidthRequest="44"
                                        StrokeShape="RoundRectangle 22"
                                        Background="{Binding PlayButtonColor}">
                                        <Border.GestureRecognizers>
                                            <TapGestureRecognizer Tapped="OnPlayButtonTapped" CommandParameter="{Binding .}"/>
                                        </Border.GestureRecognizers>
                                        <Label
                                            Text="{Binding PlayButtonText}"
                                            FontSize="18"
                                            TextColor="White"
                                            HorizontalOptions="Center"
                                            VerticalOptions="Center"/>
                                    </Border>

                                    <!-- Song Info -->
                                    <VerticalStackLayout
                                        Grid.Column="1"
                                        Spacing="4"
                                        Margin="15,0,0,0"
                                        VerticalOptions="Center">
                                        <Label
                                            Text="{Binding Title}"
                                            FontSize="16"
                                            FontAttributes="Bold"
                                            TextColor="White"
                                            MaxLines="1"
                                            LineBreakMode="TailTruncation"/>
                                        <Label
                                            Text="{Binding Artist}"
                                            FontSize="14"
                                            TextColor="#b3b3b3"
                                            MaxLines="1"
                                            LineBreakMode="TailTruncation"/>
                                    </VerticalStackLayout>

                                    <!-- Checkmark -->
                                    <Label
                                        Grid.Column="2"
                                        Text="✓"
                                        FontSize="24"
                                        TextColor="#1db954"
                                        VerticalOptions="Center"
                                        IsVisible="{Binding IsSelected}"/>

                                </Grid>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

                <!-- Loading Indicator -->
                <ActivityIndicator
                    x:Name="LoadingIndicator"
                    IsRunning="False"
                    IsVisible="False"
                    Color="#1db954"
                    HeightRequest="50"
                    Margin="0,20"/>

                <!-- Empty state -->
                <VerticalStackLayout
                    x:Name="NoResultsView"
                    IsVisible="False"
                    HorizontalOptions="Center"
                    Spacing="10"
                    Margin="0,30">
                    <Label Text="🔍" FontSize="50" HorizontalOptions="Center"/>
                    <Label Text="No songs found" FontSize="18" TextColor="White" HorizontalOptions="Center"/>
                </VerticalStackLayout>
            </VerticalStackLayout>
        </ScrollView>

        <!-- Now Playing bar -->
        <Border
            x:Name="NowPlayingBar"
            Grid.Row="4"
            BackgroundColor="#1a1a1a"
            StrokeShape="RoundRectangle 12"
            Stroke="#1db954"
            StrokeThickness="1"
            Padding="15,10"
            Margin="0,10,0,0"
            IsVisible="False">
            <Grid ColumnDefinitions="*,Auto,Auto">
                <VerticalStackLayout Grid.Column="0" VerticalOptions="Center">
                    <Label
                        x:Name="NowPlayingTitle"
                        Text=""
                        FontSize="14"
                        FontAttributes="Bold"
                        TextColor="White"
                        LineBreakMode="TailTruncation"
                        MaxLines="1"/>
                    <Label
                        x:Name="NowPlayingArtist"
                        Text=""
                        FontSize="12"
                        TextColor="#1db954"
                        LineBreakMode="TailTruncation"
                        MaxLines="1"/>
                </VerticalStackLayout>

                <!-- Pause/Resume toggle -->
                <Button
                    Grid.Column="1"
                    x:Name="PauseResumeButton"
                    Text="⏸"
                    FontSize="20"
                    TextColor="#1db954"
                    BackgroundColor="Transparent"
                    WidthRequest="44"
                    HeightRequest="44"
                    Clicked="OnPauseResumeClicked"/>

                <!-- Stop -->
                <Button
                    Grid.Column="2"
                    Text="⏹"
                    FontSize="20"
                    TextColor="#888888"
                    BackgroundColor="Transparent"
                    WidthRequest="44"
                    HeightRequest="44"
                    Clicked="OnStopButtonClicked"/>
            </Grid>
        </Border>

        <!-- Continue Button -->
        <Button
            x:Name="NextButton"
            Grid.Row="4"
            Text="Continue"
            FontSize="18"
            FontAttributes="Bold"
            TextColor="#121212"
            BackgroundColor="#535353"
            CornerRadius="25"
            HeightRequest="50"
            Margin="0,20,0,10"
            IsEnabled="False"
            Clicked="OnNextClicked"/>
    </Grid>
</ContentPage>
